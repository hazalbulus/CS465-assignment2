function createOctopusModel(gl) {
    // Define vertices, colors, and indices for the octopus head and tentacles
    var vertices = [/* ... */];
    var colors = [/* ... */];
    var indices = [/* ... */];

    // Code to create and bind VBOs and IBOs
    // Code to assign data to attributes and uniforms
}
